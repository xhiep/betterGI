<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.MapPathingDevWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:behavior="clr-namespace:BetterGenshinImpact.View.Behavior"
                 xmlns:windows="clr-namespace:BetterGenshinImpact.ViewModel.Windows"
                 xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                 d:DataContext="{d:DesignInstance Type=windows:MapPathingDevViewModel}"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 Width="320"
                 Height="260"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Auto"
                 behavior:AutoTranslateInterceptor.EnableAutoTranslate="True"
                 mc:Ignorable="d">
    <StackPanel>
        <ui:TitleBar Title="Công Cụ Dev Theo Dõi Bản Đồ">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <StackPanel Margin="12">
            <ui:TextBlock Margin="5">(Lựa chọn bản đồ bên dưới sẽ ảnh hưởng đến bản đồ độc lập nơi đường đi được ghi lại)</ui:TextBlock>
            <ui:TextBlock Margin="5">Chọn Bản Đồ:</ui:TextBlock>
            <ComboBox ItemsSource="{Binding MapTypeItems}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="EnumName"
                      SelectedValue="{Binding DevConfig.RecordMapName, Mode=TwoWay}"
                      Margin="5">
                <!--<b:Interaction.Triggers>
                    <b:EventTrigger EventName="SelectionChanged">
                        <b:InvokeCommandAction Command="{Binding DropDownChangedCommand}"
                                               CommandParameter="GeniusInvocation" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>-->
            </ComboBox>
            <ui:Button Margin="5"
                       Command="{Binding OpenMapViewerCommand}"
                       Content="Xem Bản Đồ Theo Dõi Thời Gian Thực" />

            <ui:Button Margin="5"
                       Command="{Binding OpenMapEditorCommand}"
                       Content="Trình Soạn Thảo Ghi Âm" />
        </StackPanel>
    </StackPanel>
</ui:FluentWindow>
