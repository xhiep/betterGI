<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.WelcomeDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:behavior="clr-namespace:BetterGenshinImpact.View.Behavior"
                 Width="500"
                 Height="210"
                 MinWidth="400"
                 MinHeight="230"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True"
                 behavior:AutoTranslateInterceptor.EnableAutoTranslate="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 ResizeMode="CanMinimize"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterScreen"
                 WindowStyle="SingleBorderWindow"
                 xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
                 mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Margin="12">
            <emoji:TextBlock Text="🔹 Phần mềm này mã nguồn mở và miễn phí, nếu thích hãy nhấn 🌟Star -> " Margin="5" FontSize="16">
                <Hyperlink NavigateUri="https://github.com/babalae/better-genshin-impact"
                           RequestNavigate="HyperlinkRequestNavigate" Foreground="#1E9BFA">
                    Github
                </Hyperlink>
            </emoji:TextBlock>
            <emoji:TextBlock Text="🔹 Lần đầu sử dụng vui lòng xem" Margin="5" FontSize="16">
                <Hyperlink NavigateUri="https://bettergi.com/quickstart.html"
                           RequestNavigate="HyperlinkRequestNavigate" Foreground="#1E9BFA">
                    《Hướng Dẫn Bắt Đầu Nhanh》
                </Hyperlink>
            </emoji:TextBlock>
            <emoji:TextBlock
                Margin="5"
                FontSize="16"
                TextWrapping="Wrap">
                <Run Text="❗Phần mềm này chưa bao giờ cấp phép cho bất kỳ nền tảng bên thứ ba nào để " /><Run Text="bán" Foreground="Red" />
                <Run Text=". Nếu bạn đã trả phí để có phần mềm này (qua các sàn mua bán), hãy báo cáo người bán và yêu cầu " /><Run Text="hoàn tiền" Foreground="Red" /><Run Text="!" />
            </emoji:TextBlock>
            <!--<StackPanel Margin="5"
                        Orientation="Horizontal">
                <ui:TextBlock Text="Phần mềm này " VerticalAlignment="Bottom"/>
                <emoji:TextBlock Text="🔄Mã Nguồn Mở" FontSize="20" />
                <ui:TextBlock Text="và " VerticalAlignment="Bottom"/>
                <emoji:TextBlock Text="🆓Miễn Phí" FontSize="20"/>
            </StackPanel>-->

            <StackPanel Margin="5"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <ui:Button Name="BtnOk"
                           Margin="5"
                           Appearance="Primary"
                           Click="BtnOkClick"
                           Content="Xác Nhận"
                           IsDefault="True" />
            </StackPanel>
        </StackPanel>

        <ui:TitleBar Title="BetterGI" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

    </Grid>
</ui:FluentWindow>
