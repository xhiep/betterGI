<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.SkillCdConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:skillCd="clr-namespace:BetterGenshinImpact.GameTask.SkillCd"
        mc:Ignorable="d"
        Title="Cấu Hình CD Nhân Vật"
        Height="600"
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowBackdropType="Auto"
        ExtendsContentIntoTitleBar="True">

    <ui:FluentWindow.Resources>
        <Style TargetType="ui:TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Cấu Hình CD Nhân Vật" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Settings24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="20,0,10,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 说明文本 -->
            <TextBlock Grid.Row="0" 
                       Text="Cấu hình giá trị CD mặc định khi không nhận diện được số CD cho nhân vật chỉ định. Ví dụ: Mavuika dùng E hiển thị xe máy, không nhận diện được CD.&#x0a;Nhân vật chỉ điền tên sẽ dùng CD mặc định." 
                       Margin="0,0,10,15" 
                       TextWrapping="Wrap" />

            <!-- 列表 -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          Padding="0,0,25,0">
                <ItemsControl ItemsSource="{Binding Rules}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type skillCd:SkillCdRule}">
                            <Grid Margin="0,5,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Tên Nhân Vật:" VerticalAlignment="Center" Margin="0,0,5,0" />
                                <ui:TextBox Grid.Column="1"
                                            Text="{Binding RoleName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Margin="0,0,10,0"
                                            MinWidth="120"
                                            VerticalAlignment="Center"
                                            VerticalContentAlignment="Center" />

                                <TextBlock Grid.Column="2" Text="CD (Giây):" VerticalAlignment="Center" Margin="0,0,5,0" />
                                <ui:TextBox Grid.Column="3"
                                            Text="{Binding CdValueText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            MinWidth="80"
                                            VerticalAlignment="Center"
                                            VerticalContentAlignment="Center" />

                                <ui:Button Grid.Column="4"
                                           Icon="{ui:SymbolIcon Symbol=Delete24}"
                                           Appearance="Secondary"
                                           Margin="10,0,0,0"
                                           Width="36"
                                           Padding="0"
                                           VerticalAlignment="Stretch"
                                           Click="OnDeleteClick"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="2" Margin="0,15,10,0">
                <ui:Button Content="Thêm Cấu Hình" 
                           Icon="{ui:SymbolIcon Symbol=Add24}" 
                           Appearance="Primary" 
                           HorizontalAlignment="Stretch" 
                           Click="OnAddClick" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
